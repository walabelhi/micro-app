apiVersion: v1
kind: Service
metadata:
  name: {{ include "microapp.fullname" . }}
  labels:
    {{- include "microapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "microapp.selectorLabels" . | nindent 4 }}
